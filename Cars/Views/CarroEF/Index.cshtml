@model IEnumerable<Cars.Models.Carro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_car)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.imagen_car)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Marca.nombre_marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modelo.nombre_modelo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion_car)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imagen_car)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marca.nombre_marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modelo.nombre_modelo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_car }) |
                @Html.ActionLink("Details", "Details", new { id = item.id_car }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id_car })
            </td>
        </tr>
    }

</table>
